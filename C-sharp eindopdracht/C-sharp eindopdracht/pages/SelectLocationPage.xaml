<Page
    x:Class="C_sharp_eindopdracht.pages.SelectLocationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:C_sharp_eindopdracht.Model"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Page.Resources>
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Comic Sans MS"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Page.Resources>

    <Page.Background>
        <ImageBrush Stretch="UniformToFill" ImageSource="ms-appx:///Assets/achtergrond.png" Opacity="0.8"/>
    </Page.Background>

    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.Content>
                <Grid/>
            </CommandBar.Content>
            <AppBarButton Icon="Home" Click="AppBarButton_Click" Label="terug"/>
        </CommandBar>
        
    </Page.BottomAppBar>
    <RelativePanel>
        <RelativePanel.Background>
            <ImageBrush Stretch="Fill"/>
        </RelativePanel.Background>

        <TextBox x:Name="InputField" Grid.Row="0" TextChanged="InputField_TextChanged" HorizontalAlignment="Stretch" RelativePanel.AlignRightWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignTopWithPanel="True" Margin="0,-2,0,2" UseSystemFocusVisuals="True"></TextBox>

        <ScrollViewer x:Name="listView" Margin="0,40,0,0" RelativePanel.AlignRightWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignBottomWithPanel="True" >
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <ListView Name="locations"
                          ItemsSource="{x:Bind model.publicLocations}"
                          ItemClick="locations_ItemClick"
                          IsItemClickEnabled="True">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:Location">
                            <Grid HorizontalAlignment="Stretch">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition MinWidth="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{x:Bind Name, Mode =TwoWay}" Grid.Column="1" Grid.Row="0" Margin="5" FontWeight="Bold"/>
                                <TextBlock Text="{x:Bind type, Mode=TwoWay}" VerticalAlignment="Center" Grid.RowSpan="2" Foreground="{ThemeResource SystemControlBackgroundAccentBrush}" Margin="5"  Grid.Column="0"/>
                                <TextBlock Text="{x:Bind id,Mode=TwoWay}" Foreground="Gray" Grid.Row="2" Grid.Column="2"  Margin="5"  />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </ScrollViewer>
    </RelativePanel>
</Page>
